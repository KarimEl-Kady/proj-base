apiVersion: v1
kind: Service
metadata:
  name: proj-base-app
  labels:
    app.kubernetes.io/name: proj-base
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: proj-base
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: 80
# Put your cluster's Ingress (TLS termination, hostnames) in front of this
# Service — deliberately not included here, see deploy/k8s/README.md.
