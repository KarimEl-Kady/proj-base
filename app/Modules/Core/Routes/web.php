<?php

/*
|--------------------------------------------------------------------------
| Core Web Routes
|--------------------------------------------------------------------------
|
| Loaded under the "web" middleware group by CoreServiceProvider when
| project.route_attributes.enabled is false. Core has no web controllers
| by default — this file exists so the convention is visible; add routes
| here if the Core module ever needs one.
|
*/
